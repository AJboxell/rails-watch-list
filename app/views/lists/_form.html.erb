<% @list = List.find(params[:id]) %>
<% @bookmark = Bookmark.new %>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit %>
<% end %>
